/**
 * Light-mode accent hex per case study, kept in sync with the CSS overrides
 * in app/globals.css. Used where a literal colour value is needed (e.g.
 * generated OpenGraph images), which can't read CSS custom properties.
 */
export const projectAccentHex: Record<string, string> = {
  tyread: "#b45309",
  "zurickh-bank": "#0f766e",
  "kols-investment": "#047857",
};

export const DEFAULT_ACCENT_HEX = "#1d4ed8";
